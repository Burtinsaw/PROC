# Frontend Production Environment Example
# Copy to .env.production and adjust.

# Base API endpoint (without trailing slash). If behind same domain with /api proxy, can omit.
VITE_APP_API_URL=https://api.example.com/api
# Application public URL (optional for share links)
VITE_APP_PUBLIC_URL=https://app.example.com
# Build-time flags
VITE_APP_BUILD_LABEL=prod
